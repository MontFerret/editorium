// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: ferretd/daemon/v1/daemon.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import type { handleUnaryCall, UntypedServiceImplementation } from "@grpc/grpc-js";

export const protobufPackage = "ferretd.daemon.v1";

export interface ApiVersion {
  major: number;
  minor: number;
}

export interface GetInfoRequest {
  clientApi?: ApiVersion | undefined;
}

export interface ServerInfo {
  version: string;
  instanceId: string;
  apiVersion?: ApiVersion | undefined;
}

export interface GetInfoResponse {
  serverInfo?: ServerInfo | undefined;
}

export interface ApiCompatibilityError {
  clientApi?: ApiVersion | undefined;
  serverApi?: ApiVersion | undefined;
}

export interface ShutdownRequest {
}

export interface ShutdownResponse {
}

function createBaseApiVersion(): ApiVersion {
  return { major: 0, minor: 0 };
}

export const ApiVersion: MessageFns<ApiVersion> = {
  encode(message: ApiVersion, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.major !== 0) {
      writer.uint32(8).uint32(message.major);
    }
    if (message.minor !== 0) {
      writer.uint32(16).uint32(message.minor);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ApiVersion {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseApiVersion();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.major = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.minor = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ApiVersion>): ApiVersion {
    return ApiVersion.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ApiVersion>): ApiVersion {
    const message = createBaseApiVersion();
    message.major = object.major ?? 0;
    message.minor = object.minor ?? 0;
    return message;
  },
};

function createBaseGetInfoRequest(): GetInfoRequest {
  return { clientApi: undefined };
}

export const GetInfoRequest: MessageFns<GetInfoRequest> = {
  encode(message: GetInfoRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.clientApi !== undefined) {
      ApiVersion.encode(message.clientApi, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetInfoRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetInfoRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.clientApi = ApiVersion.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<GetInfoRequest>): GetInfoRequest {
    return GetInfoRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetInfoRequest>): GetInfoRequest {
    const message = createBaseGetInfoRequest();
    message.clientApi = (object.clientApi !== undefined && object.clientApi !== null)
      ? ApiVersion.fromPartial(object.clientApi)
      : undefined;
    return message;
  },
};

function createBaseServerInfo(): ServerInfo {
  return { version: "", instanceId: "", apiVersion: undefined };
}

export const ServerInfo: MessageFns<ServerInfo> = {
  encode(message: ServerInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.version !== "") {
      writer.uint32(10).string(message.version);
    }
    if (message.instanceId !== "") {
      writer.uint32(18).string(message.instanceId);
    }
    if (message.apiVersion !== undefined) {
      ApiVersion.encode(message.apiVersion, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ServerInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServerInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.version = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.instanceId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.apiVersion = ApiVersion.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ServerInfo>): ServerInfo {
    return ServerInfo.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ServerInfo>): ServerInfo {
    const message = createBaseServerInfo();
    message.version = object.version ?? "";
    message.instanceId = object.instanceId ?? "";
    message.apiVersion = (object.apiVersion !== undefined && object.apiVersion !== null)
      ? ApiVersion.fromPartial(object.apiVersion)
      : undefined;
    return message;
  },
};

function createBaseGetInfoResponse(): GetInfoResponse {
  return { serverInfo: undefined };
}

export const GetInfoResponse: MessageFns<GetInfoResponse> = {
  encode(message: GetInfoResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.serverInfo !== undefined) {
      ServerInfo.encode(message.serverInfo, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetInfoResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetInfoResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.serverInfo = ServerInfo.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<GetInfoResponse>): GetInfoResponse {
    return GetInfoResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetInfoResponse>): GetInfoResponse {
    const message = createBaseGetInfoResponse();
    message.serverInfo = (object.serverInfo !== undefined && object.serverInfo !== null)
      ? ServerInfo.fromPartial(object.serverInfo)
      : undefined;
    return message;
  },
};

function createBaseApiCompatibilityError(): ApiCompatibilityError {
  return { clientApi: undefined, serverApi: undefined };
}

export const ApiCompatibilityError: MessageFns<ApiCompatibilityError> = {
  encode(message: ApiCompatibilityError, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.clientApi !== undefined) {
      ApiVersion.encode(message.clientApi, writer.uint32(10).fork()).join();
    }
    if (message.serverApi !== undefined) {
      ApiVersion.encode(message.serverApi, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ApiCompatibilityError {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseApiCompatibilityError();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.clientApi = ApiVersion.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.serverApi = ApiVersion.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ApiCompatibilityError>): ApiCompatibilityError {
    return ApiCompatibilityError.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ApiCompatibilityError>): ApiCompatibilityError {
    const message = createBaseApiCompatibilityError();
    message.clientApi = (object.clientApi !== undefined && object.clientApi !== null)
      ? ApiVersion.fromPartial(object.clientApi)
      : undefined;
    message.serverApi = (object.serverApi !== undefined && object.serverApi !== null)
      ? ApiVersion.fromPartial(object.serverApi)
      : undefined;
    return message;
  },
};

function createBaseShutdownRequest(): ShutdownRequest {
  return {};
}

export const ShutdownRequest: MessageFns<ShutdownRequest> = {
  encode(_: ShutdownRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ShutdownRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseShutdownRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ShutdownRequest>): ShutdownRequest {
    return ShutdownRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<ShutdownRequest>): ShutdownRequest {
    const message = createBaseShutdownRequest();
    return message;
  },
};

function createBaseShutdownResponse(): ShutdownResponse {
  return {};
}

export const ShutdownResponse: MessageFns<ShutdownResponse> = {
  encode(_: ShutdownResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ShutdownResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseShutdownResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ShutdownResponse>): ShutdownResponse {
    return ShutdownResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<ShutdownResponse>): ShutdownResponse {
    const message = createBaseShutdownResponse();
    return message;
  },
};

export type DaemonServiceService = typeof DaemonServiceService;
export const DaemonServiceService = {
  getInfo: {
    path: "/ferretd.daemon.v1.DaemonService/GetInfo" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: GetInfoRequest): Buffer => Buffer.from(GetInfoRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): GetInfoRequest => GetInfoRequest.decode(value),
    responseSerialize: (value: GetInfoResponse): Buffer => Buffer.from(GetInfoResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): GetInfoResponse => GetInfoResponse.decode(value),
  },
  shutdown: {
    path: "/ferretd.daemon.v1.DaemonService/Shutdown" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ShutdownRequest): Buffer => Buffer.from(ShutdownRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ShutdownRequest => ShutdownRequest.decode(value),
    responseSerialize: (value: ShutdownResponse): Buffer => Buffer.from(ShutdownResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ShutdownResponse => ShutdownResponse.decode(value),
  },
} as const;

export interface DaemonServiceServer extends UntypedServiceImplementation {
  getInfo: handleUnaryCall<GetInfoRequest, GetInfoResponse>;
  shutdown: handleUnaryCall<ShutdownRequest, ShutdownResponse>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
