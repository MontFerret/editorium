// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: ferretd/workspace/v1/workspace.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import type { handleUnaryCall, UntypedServiceImplementation } from "@grpc/grpc-js";

export const protobufPackage = "ferretd.workspace.v1";

export interface WorkspaceId {
  value: string;
}

export interface Workspace {
  id?: WorkspaceId | undefined;
  root: string;
}

export interface OpenRequest {
  root: string;
}

export interface OpenResponse {
  workspace?: Workspace | undefined;
}

export interface GetRequest {
  id?: WorkspaceId | undefined;
}

export interface GetResponse {
  workspace?: Workspace | undefined;
}

export interface ListRequest {
}

export interface ListResponse {
  workspaces: Workspace[];
}

export interface CloseRequest {
  id?: WorkspaceId | undefined;
}

export interface CloseResponse {
}

function createBaseWorkspaceId(): WorkspaceId {
  return { value: "" };
}

export const WorkspaceId: MessageFns<WorkspaceId> = {
  encode(message: WorkspaceId, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.value !== "") {
      writer.uint32(10).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WorkspaceId {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWorkspaceId();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<WorkspaceId>): WorkspaceId {
    return WorkspaceId.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<WorkspaceId>): WorkspaceId {
    const message = createBaseWorkspaceId();
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseWorkspace(): Workspace {
  return { id: undefined, root: "" };
}

export const Workspace: MessageFns<Workspace> = {
  encode(message: Workspace, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      WorkspaceId.encode(message.id, writer.uint32(10).fork()).join();
    }
    if (message.root !== "") {
      writer.uint32(18).string(message.root);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Workspace {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWorkspace();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = WorkspaceId.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.root = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<Workspace>): Workspace {
    return Workspace.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Workspace>): Workspace {
    const message = createBaseWorkspace();
    message.id = (object.id !== undefined && object.id !== null) ? WorkspaceId.fromPartial(object.id) : undefined;
    message.root = object.root ?? "";
    return message;
  },
};

function createBaseOpenRequest(): OpenRequest {
  return { root: "" };
}

export const OpenRequest: MessageFns<OpenRequest> = {
  encode(message: OpenRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.root !== "") {
      writer.uint32(10).string(message.root);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): OpenRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOpenRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.root = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<OpenRequest>): OpenRequest {
    return OpenRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<OpenRequest>): OpenRequest {
    const message = createBaseOpenRequest();
    message.root = object.root ?? "";
    return message;
  },
};

function createBaseOpenResponse(): OpenResponse {
  return { workspace: undefined };
}

export const OpenResponse: MessageFns<OpenResponse> = {
  encode(message: OpenResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.workspace !== undefined) {
      Workspace.encode(message.workspace, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): OpenResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOpenResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.workspace = Workspace.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<OpenResponse>): OpenResponse {
    return OpenResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<OpenResponse>): OpenResponse {
    const message = createBaseOpenResponse();
    message.workspace = (object.workspace !== undefined && object.workspace !== null)
      ? Workspace.fromPartial(object.workspace)
      : undefined;
    return message;
  },
};

function createBaseGetRequest(): GetRequest {
  return { id: undefined };
}

export const GetRequest: MessageFns<GetRequest> = {
  encode(message: GetRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      WorkspaceId.encode(message.id, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = WorkspaceId.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<GetRequest>): GetRequest {
    return GetRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetRequest>): GetRequest {
    const message = createBaseGetRequest();
    message.id = (object.id !== undefined && object.id !== null) ? WorkspaceId.fromPartial(object.id) : undefined;
    return message;
  },
};

function createBaseGetResponse(): GetResponse {
  return { workspace: undefined };
}

export const GetResponse: MessageFns<GetResponse> = {
  encode(message: GetResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.workspace !== undefined) {
      Workspace.encode(message.workspace, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.workspace = Workspace.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<GetResponse>): GetResponse {
    return GetResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetResponse>): GetResponse {
    const message = createBaseGetResponse();
    message.workspace = (object.workspace !== undefined && object.workspace !== null)
      ? Workspace.fromPartial(object.workspace)
      : undefined;
    return message;
  },
};

function createBaseListRequest(): ListRequest {
  return {};
}

export const ListRequest: MessageFns<ListRequest> = {
  encode(_: ListRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ListRequest>): ListRequest {
    return ListRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<ListRequest>): ListRequest {
    const message = createBaseListRequest();
    return message;
  },
};

function createBaseListResponse(): ListResponse {
  return { workspaces: [] };
}

export const ListResponse: MessageFns<ListResponse> = {
  encode(message: ListResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.workspaces) {
      Workspace.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.workspaces.push(Workspace.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<ListResponse>): ListResponse {
    return ListResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListResponse>): ListResponse {
    const message = createBaseListResponse();
    message.workspaces = object.workspaces?.map((e) => Workspace.fromPartial(e)) || [];
    return message;
  },
};

function createBaseCloseRequest(): CloseRequest {
  return { id: undefined };
}

export const CloseRequest: MessageFns<CloseRequest> = {
  encode(message: CloseRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      WorkspaceId.encode(message.id, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CloseRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCloseRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = WorkspaceId.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<CloseRequest>): CloseRequest {
    return CloseRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CloseRequest>): CloseRequest {
    const message = createBaseCloseRequest();
    message.id = (object.id !== undefined && object.id !== null) ? WorkspaceId.fromPartial(object.id) : undefined;
    return message;
  },
};

function createBaseCloseResponse(): CloseResponse {
  return {};
}

export const CloseResponse: MessageFns<CloseResponse> = {
  encode(_: CloseResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CloseResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCloseResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create(base?: DeepPartial<CloseResponse>): CloseResponse {
    return CloseResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<CloseResponse>): CloseResponse {
    const message = createBaseCloseResponse();
    return message;
  },
};

export type WorkspaceServiceService = typeof WorkspaceServiceService;
export const WorkspaceServiceService = {
  open: {
    path: "/ferretd.workspace.v1.WorkspaceService/Open" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: OpenRequest): Buffer => Buffer.from(OpenRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): OpenRequest => OpenRequest.decode(value),
    responseSerialize: (value: OpenResponse): Buffer => Buffer.from(OpenResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): OpenResponse => OpenResponse.decode(value),
  },
  get: {
    path: "/ferretd.workspace.v1.WorkspaceService/Get" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: GetRequest): Buffer => Buffer.from(GetRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): GetRequest => GetRequest.decode(value),
    responseSerialize: (value: GetResponse): Buffer => Buffer.from(GetResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): GetResponse => GetResponse.decode(value),
  },
  list: {
    path: "/ferretd.workspace.v1.WorkspaceService/List" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ListRequest): Buffer => Buffer.from(ListRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ListRequest => ListRequest.decode(value),
    responseSerialize: (value: ListResponse): Buffer => Buffer.from(ListResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ListResponse => ListResponse.decode(value),
  },
  close: {
    path: "/ferretd.workspace.v1.WorkspaceService/Close" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: CloseRequest): Buffer => Buffer.from(CloseRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): CloseRequest => CloseRequest.decode(value),
    responseSerialize: (value: CloseResponse): Buffer => Buffer.from(CloseResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): CloseResponse => CloseResponse.decode(value),
  },
} as const;

export interface WorkspaceServiceServer extends UntypedServiceImplementation {
  open: handleUnaryCall<OpenRequest, OpenResponse>;
  get: handleUnaryCall<GetRequest, GetResponse>;
  list: handleUnaryCall<ListRequest, ListResponse>;
  close: handleUnaryCall<CloseRequest, CloseResponse>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
